# podium-drive2
same basic functionality as the old files but I'm using an uno r4 wifi instead of the r3. There was some weirdness between the two platforms that needed tending to so most of the code has been altered aside from the header file for the joystick parser. 
